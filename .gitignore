.env.local
venv/
venv-*/
.DS_Store
test.py
twilio.env
.vscode/settings.json

# LiveKit CLI binaries and downloads
lk
lk.tar.gz
autocomplete/

# Python cache
__pycache__/

# SIP trunk configuration (contains credentials)
outbound_trunk.json
inbound_trunk.json
dispatch_rule.json

# Voice testing sandbox app (generated by lk CLI)
voice-test/

# Runtime call logs / transcripts (contains PII + health info)
calls.db
*.db
# SQLite write-ahead-log / shared-memory temp files
*.db-shm
*.db-wal

# Node deps (not used at runtime — pure-Python project; never commit this)
node_modules/

# Crash dumps
*.stackdump
